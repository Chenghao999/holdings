# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg

# 虚拟环境
.venv/
venv/
env/
ENV/

# 测试与覆盖率
.pytest_cache/
.coverage
.coverage.*
htmlcov/
.tox/
.nox/

# 代码检查与类型缓存
.ruff_cache/
.mypy_cache/
.dmypy.json
.pyre/

# 运行时本地数据（用户私有，不入库）
# 注意：必须加前导斜杠锚定到具体目录。写成 `data/` 会匹配任意层级的 data
# 目录，把源码包 src/holdings/data/ 一并排除掉（曾因此漏提交整个包）。
/data/
/snapshots/
/_smoke/data/
config.yaml

# 图表 HTML 导出（`holdings chart` 的默认产物落在仓库根）
# 同上面 `/data/` 的道理：写成 `*.html` 会连源码包里的 .html 一起排除——
# web 层的 Jinja 模板就是 .html，曾因此漏提交整个 templates/，
# 本地测试全绿（文件在磁盘上）而 CI 一请求就 TemplateNotFound。
/networth.html

# 演示录制的原始文件（GIF 由它渲染而来，源码是 docs/demo/demo.sh）
docs/demo/*.cast

# 操作系统 / 编辑器
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp